$(document).ready(function () {
    $('.slider').slick({
        
        dots: true,
        // draggable: true,
        // swipe: true
        // autoplay: true,
        // autoplaySpeed: 2000,
        arrows: true,
        appendDots: $('.slider-image'),
        appendArrows: $('.slider-image'),
  });
});